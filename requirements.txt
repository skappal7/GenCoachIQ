# Minimal GenCoachingIQ Requirements
# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# File processing
openpyxl>=3.1.0

# NLP - Advanced (optional but recommended)
transformers>=4.30.0
torch>=2.0.0
tokenizers>=0.13.0

# NLP - Fallback
textblob>=0.17.1

# Machine Learning for theme extraction
scikit-learn>=1.3.0

# High-performance data processing (optional but recommended for large files)
pyarrow>=12.0.0

# Standard library dependencies (usually included with Python)
# json (built-in)
# re (built-in) 
# logging (built-in)
# datetime (built-in)
# argparse (built-in)
# io (built-in)
